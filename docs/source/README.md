# Source architecture drawing

`FUNDAMENTAL_ARCHITECTURE_FIG_1A-1C.pdf` is the three-page foundational architecture drawing supplied by the repository author as the implementation source for this reference package.

The executable mapping is documented in `../ARCHITECTURE.md` and `../IMPLEMENTATION_DETAILS.md`.

The drawing is included so reviewers can compare the code against the stated sequence instead of relying on a prose reinterpretation. Inclusion of this file does not alter the licensing/patent notice in the repository root.
